# Serylane 官网品牌素材来源

使用方式：内置 image_gen（不是 CLI / API fallback）。输入为已确认的首页概念图；输出为独立透明 S 标志。网站沿用该标志，不新增独立的页脚 Logo。

- 原始生成 PNG：1254 × 1254 RGBA；已确认角点 alpha=0。
- 项目源图：`../../assets/brand/serylane-icon.png`。
- 源图 SHA-256：`3027336ffc8474cc276b31cd30d240bed29120f8e5450d8cd284bd645537f570`。
- Tauri 官方图标格式转换后，官网使用 256 px PNG（正文与分享摘要），32 px PNG（favicon）。不是用代码重绘或修改标志。

生成提示词原文：

```text
Use case: background-extraction. The input is an approved Serylane website concept. Produce a standalone, production brand-mark asset from ONLY the small blue angular S route-shaped symbol in the TOP-LEFT website navigation, immediately before the word Serylane. Preserve its exact silhouette, pointed tapered ends, folded central path, narrow proportions, and restrained vivid blue gradient. Do NOT use any other symbol from the product mockup. Output a square 1024x1024 PNG with a GENUINELY TRANSPARENT alpha background, just this isolated blue S symbol centered and occupying about 82 percent of the image height with even outer padding. No wordmark, text, UI, glass panel, border, cast shadow or decorative elements. Crisp smooth antialiased edges suitable for an official website, favicon and desktop application icon source. Match the approved mark, do not redesign it or turn it into a flowing ribbon, lightning bolt, generic letter S, or new logo.
```

提示词请求 1024 px；实际生成结果为 1254 px，以真实文件尺寸为准。批准稿中的页面结构、正文和玻璃层级已原生实现；分享摘要图只使用品牌标志，不展示虚假的在线状态或性能数值。
