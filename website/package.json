{
  "name": "serylane-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check-site.mjs",
    "test": "node scripts/check-site.mjs && node scripts/test-downloads.mjs && node --test scripts/test-worker.mjs scripts/test-deploy-site.mjs",
    "typecheck": "wrangler types && tsc --noEmit",
    "dev": "node scripts/serve.mjs",
    "deploy:check": "wrangler deploy --dry-run",
    "deploy": "pnpm test && pnpm typecheck && node scripts/deploy-site.mjs"
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "wrangler": "4.129.0"
  }
}
